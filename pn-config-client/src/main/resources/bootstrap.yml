

spring:
  cloud:
    config:
      uri: http://localhost:8088/
      #name: cloud-config
      profile: pro
      label: master
      #对应config server文件名前缀如： client1-test.yml
  application:
    name: client1